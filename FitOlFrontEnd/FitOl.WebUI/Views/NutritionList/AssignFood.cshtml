@model List<AssignFoodModel>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<form asp-action="AssignFood" method="POST">
    @for (int i = 0; i < Model.Count; i++)
    {
        <div class="form-check">
            <input type="hidden" asp-for="@Model[i].FoodName">
            <input type="hidden" asp-for="@Model[i].FoodId">

            <input asp-for="@Model[i].Exists" class="form-check-input" type="checkbox">
            <label asp-for="@Model[i].Exists" class="form-check-label">@Model[i].FoodName</label>
        </div>
    }
    <button type="submit" class="btn btn-primary btn-sm">Kaydet</button>
</form>